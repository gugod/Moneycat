#!/usr/bin/env perl
use v5.14;
use strict;
use warnings;
use YAML;
use Moneycat;

say YAML::Dump(Moneycat->fetch_currency_exchange_rate_from_esunbank);
say YAML::Dump(Moneycat->fetch_currency_exchange_rate_from_bot);
